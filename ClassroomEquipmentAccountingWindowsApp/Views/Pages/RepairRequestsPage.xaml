<Page x:Class="ClassroomEquipmentAccountingWindowsApp.Views.Pages.RepairRequestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClassroomEquipmentAccountingWindowsApp.Views.Pages"
      xmlns:vm="clr-namespace:ClassroomEquipmentAccountingWindowsApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RepairRequestsPage">
    <Page.DataContext>
        <vm:RepairRequestsPageViewModel/>
    </Page.DataContext>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="+" Margin="10" x:Name="AddButton"
                    Command="{Binding AddCommand}" IsEnabled="{Binding CanAdd}"/>
            
            <Button Grid.Column="1" Content="Pr" Margin="10"
                    Command="{Binding CreateInventoryCommand}" IsEnabled="{Binding CanCreateInventory}"/>

            <TextBox Grid.Column="2" Margin="10" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" x:Name="SearchTextBox"/>

            <ComboBox Grid.Column="3" Margin="10" x:Name="FiltherComboBox"
                      ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"/>

            <ComboBox Grid.Column="4" Margin="10" x:Name="SortComboBox"
                      ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SelectedSort, Mode=TwoWay}"/>

        </Grid>

        <ListView Grid.Row="1" Margin="10"
                  ItemsSource="{Binding ItemsView}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                    <Setter Property="Padding" Value="6"/>
                    <Setter Property="Margin" Value="0,6,0,0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                                    <ContentPresenter Margin="4"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource WindowSecondaryColor}"/>
                                        <Setter TargetName="Bd" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="6" Opacity="0.12" ShadowDepth="2"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource WindowTertiaryColor}"/>
                                        <Setter TargetName="Bd" Property="RenderTransform">
                                            <Setter.Value>
                                                <TranslateTransform Y="1"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{StaticResource WindowTertiaryColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="Transparent" CornerRadius="6" Padding="8">
                        <StackPanel>
                            <TextBlock Text="{Binding Id, StringFormat=[{0}]}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding StartDate, StringFormat=Начало: {0:dd.MM.yyyy}}" />
                            <TextBlock Text="{Binding EndDate, StringFormat=Окончание: {0:dd.MM.yyyy}}" />
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Text="{Binding Items.Count, StringFormat={}{0} элементов}" FontSize="20"/>

    </Grid>
    
</Page>
