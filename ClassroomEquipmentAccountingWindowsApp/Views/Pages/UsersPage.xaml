<Page x:Class="ClassroomEquipmentAccountingWindowsApp.Views.Pages.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClassroomEquipmentAccountingWindowsApp.Views.Pages"
      xmlns:vm="clr-namespace:ClassroomEquipmentAccountingWindowsApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UsersPage">
    <Page.DataContext>
        <vm:UsersPageViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="+" Margin="10" Command="{Binding AddUserCommand}"/>
            <TextBox Grid.Column="1" Margin="10" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <ComboBox Grid.Column="2" Margin="10" ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SelectedSort}"/>
            <ComboBox Grid.Column="3" Margin="10" ItemsSource="{Binding TagFilterOptions}" SelectedItem="{Binding SelectedTagFilter}"/>
        </Grid>

        <ListView Grid.Row="1"
                  Margin="10"
                  SelectedItem="{Binding SelectedUser, UpdateSourceTrigger=PropertyChanged}"
                  ItemsSource="{Binding UsersView}">
            <ListView.InputBindings>
                <KeyBinding Key="Delete" Command="{Binding DeleteUserCommand}" />
            </ListView.InputBindings>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <TextBlock Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Users.Count, StringFormat='Всего: {0}'}" FontSize="20"/>
    </Grid>
</Page>
